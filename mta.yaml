_schema-version: "3.2"
ID: maintnotimaint
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: maintnotimaint-destination-content
  type: com.sap.application.content
  requires:
  - name: maintnotimaint-destination-service
    parameters:
      content-target: true
  - name: maintnotimaint-repo-host
    parameters:
      service-key:
        name: maintnotimaint-repo-host-key
  - name: maintnotimaint-uaa
    parameters:
      service-key:
        name: maintnotimaint-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: maintnotimaint_html_repo_host
          ServiceInstanceName: maintnotimaint-html5-service
          ServiceKeyName: maintnotimaint-repo-host-key
          sap.cloud.service: maintnotimaint
        - Authentication: OAuth2UserTokenExchange
          Name: maintnotimaint_uaa
          ServiceInstanceName: maintnotimaint-xsuaa-service
          ServiceKeyName: maintnotimaint-uaa-key
          sap.cloud.service: maintnotimaint
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: maintnotimaint-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: maintnotimaint-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - maintnotimaint.zip
      name: maintnotimaint
      target-path: resources/
- name: maintnotimaint
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: maintnotimaint-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: maintnotimaint-destination-service
    service-plan: lite
- name: maintnotimaint-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: maintnotimaint-xsuaa-service
    service-plan: application
- name: maintnotimaint-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: maintnotimaint-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
